<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.visanka.bschatz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsAppChat"
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic">
        <activity
            android:name=".Retrive"
            android:exported="false" />
        <activity
            android:name=".LikeCommentPost"
            android:exported="false" />
        <activity
            android:name=".createSavePost"
            android:exported="false" />
        <activity android:name=".UsersActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ChatsActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name="com.visanka.bschatz.MainActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>